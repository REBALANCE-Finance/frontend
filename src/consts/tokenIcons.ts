import { ICON_NAMES } from "./iconNames";

export const TOKEN_ICONS: Record<string, string> = {
  usdt: ICON_NAMES.usdt,
  usdc: ICON_NAMES.usdc,
  dai: ICON_NAMES.dai,
  frax: ICON_NAMES.frax,
  wbtc: ICON_NAMES.wbtc,
  weth: ICON_NAMES.weth
};
